{
  "name": "cf-error-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && bun run routes",
    "deploy:worker": "wrangler deploy",
    "routes": "bun run scripts/attach-all-zones.ts",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0"
  }
}